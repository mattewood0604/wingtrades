import Vue from 'vue';
import { createRouter } from './router';

import './style/util.scss';

// tslint:disable-next-line:no-unused-expression
new Vue({
  el: '#app-main',
  router: createRouter(),
  components: {

  }
});
